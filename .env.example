# CreditLens Environment Variables
# Copy this file to .env and fill in values

# API endpoint (where the FastAPI server runs)
API_BASE_URL=your_api_url

# Ollama model name (auto-detected if not set)
# Free options: llama3.2, llama3.1, mistral, gemma2, phi3
MODEL_NAME=llama3.2

# Hugging Face token (only needed for HF Spaces deployment)
HF_TOKEN=hf_your_huggingface_token_here
